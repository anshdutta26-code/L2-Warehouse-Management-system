# Copy to .streamlit/secrets.toml locally, or paste into hosting Secrets.
# NEVER upload a real secrets.toml to a repository.
backend_url = "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"
api_secret = "COPY_API_SECRET_FROM_APPS_SCRIPT_PROPERTIES"

[users.ansh]
password_hash = "PASTE_OUTPUT_FROM_make_password.py"
role = "admin"

# Each operator gets a separate login for the audit trail.
# [users.operator1]
# password_hash = "ANOTHER_PASSWORD_HASH"
# role = "operator"
